# بديل: نشر على فرع gh-pages (لو اخترت Deploy from a branch في Settings > Pages)
# معطّل لأننا نستخدم Deploy CV to GitHub Pages (Source: GitHub Actions)
name: Deploy CV to gh-pages branch

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          cache: true

      - name: Build Flutter Web
        run: |
          flutter pub get
          flutter build web --base-href "/${{ github.event.repository.name }}/"

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: build/web
          publish_branch: gh-pages
